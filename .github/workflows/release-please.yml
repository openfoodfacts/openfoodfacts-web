 name: Run release-please
 on:
   push:
     branches:
       - main
 jobs:
   release-please:
     runs-on: ubuntu-slim
     steps:
       - uses: googleapis/release-please-action@v4.4.0
         with:
           token: ${{ secrets.GITHUB_TOKEN }}
           release-type: simple
